apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
# Local build config for the minikube context.
#  local:
#    push: false
# Google Cloud config for the gCloud context.
  googleCloudBuild:
    projectId: ticketing-dev-379109
  artifacts:
    - image: us.gcr.io/ticketing-dev-379109/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .